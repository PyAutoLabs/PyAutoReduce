"""
Packaging: cut out the lens, and write the modeling-ready dataset —
``data.fits``, ``noise_map.fits``, ``psf.fits``, ``psf_full.fits`` and the
``reduction.json`` provenance record (program IDs, exposures, zero-point,
exposure time, pixel scale, pipeline versions).
"""
